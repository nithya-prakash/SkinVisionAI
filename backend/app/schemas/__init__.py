"""Pydantic v2 domain schemas.

These are the DTOs used at API boundaries and as the typed contracts that
future CV, ingredient-engine, and agent/tool components will produce and
consume. Nothing in this package performs I/O or business logic — it is
pure data-shape definition and validation.
"""
